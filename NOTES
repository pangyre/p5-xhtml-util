Global setup:

  Download and install Git
  git config --global user.name "Ashley Pond V"
  git config --global user.email ashley.pond.v@gmail.com
        

Next steps:

  mkdir p5-xhtml-util
  cd p5-xhtml-util
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:pangyre/p5-xhtml-util.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:pangyre/p5-xhtml-util.git
  git push origin master

